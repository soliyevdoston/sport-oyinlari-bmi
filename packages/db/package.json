{
  "name": "@aetherscore/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Prisma schema package has no build output'",
    "prisma:format": "prisma format",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "lint": "echo 'No lint configured for db package yet'"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
